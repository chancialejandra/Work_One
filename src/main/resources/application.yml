server:
  servlet:
    context-path: /workOne/v1
  port: 9080
spring:
  application:
    name: todo-app
  datasource:
    driver-class-name: org.h2.Driver
    username: achanci
    password: 1234
    url: "jdbc:h2:mem:work-one"
  h2:
    console:
      enabled: true
  jpa:
    show-sql: true
    defer-datasource-initialization: true
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
